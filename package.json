{
  "scripts": {
    "start:all": "concurrently \"npm run start:dev --prefix api-gateway\" \"npm run:dev start --prefix mailer-service\" \"npm run start:dev --prefix users-service\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
